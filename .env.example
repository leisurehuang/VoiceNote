# Voice Notes 配置。复制为 .env 后按需修改（绝大多数情况下用默认值即可）。

PORT=3000

# ---- whisper.cpp ----
# whisper-cli 是 keg-only，默认不在 PATH。默认值通过 `brew --prefix whisper-cpp` 自动解析。
# WHISPER_CLI=/opt/homebrew/opt/whisper-cpp/bin/whisper-cli
# 模型文件路径（brew 不带模型，需运行 `npm run setup` 下载）
# 默认 turbo（~1.5GB，质量接近 large、速度快、内存占用小）；要换 large-v3 改文件名即可
WHISPER_MODEL=/Users/lei/.voice-notes-models/ggml-large-v3-turbo.bin
WHISPER_THREADS=8
# 默认 auto（自动识别语种，中英文等都支持）。仅当自动识别总不准时才固定为 zh/en 等
WHISPER_LANGUAGE=auto
# 解码预热提示，留空=不偏置（多语种默认）。纯中文场景可设「以下是普通话的句子。」提升中文一致性
WHISPER_PROMPT=

# ---- 摘要 LLM（OpenAI 兼容；默认本地 Ollama，可改成任意云 API）----
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=qwen2.5:7b-instruct
OLLAMA_API_KEY=ollama

# 以上三项启动时作为「默认」LLM 预设；运行时在应用内「⚙ 设置」页可保存多套预设并切换（优先级高于此处）。

# 可选：实时增量摘要用的模型（缺省跟随 OLLAMA_MODEL）
# OLLAMA_INCREMENTAL_MODEL=qwen2.5:7b-instruct
# 实时增量摘要的刷新节奏：新增多少字符触发一次 / 两次刷新的最小间隔(毫秒)
# INCREMENTAL_THRESHOLD_CHARS=280
# INCREMENTAL_MIN_INTERVAL_MS=8000
